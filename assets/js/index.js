const express = require('express')
const app = express()
const port = 4000

app.get ('/'halo wawan (req, res) => { 
  res.send ('Hello Ganteng!')
})
app.listen(port.() => {
  res.send ("Pinjem duit bro")
})

app.listen (port.(4000 ) => {
  console.log("server listening on port 4000");
})